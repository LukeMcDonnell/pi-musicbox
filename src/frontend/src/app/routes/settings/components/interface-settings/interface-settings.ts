import { Component } from '@angular/core';

/** The Interface tab. A placeholder until there is something to put on it. */
@Component({
    selector: 'app-interface-settings',
    template: `
        <h2 class="text-lg font-semibold">Interface</h2>
        <p class="pt-2 text-[0.95rem] text-muted">
            How the panel and a phone present themselves. Nothing to set here yet.
        </p>
    `,
})
export class InterfaceSettings {}
